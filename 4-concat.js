const firstArg = process.argv[2];
const secondArg = process.argv[3];

console.log(firstArg + " is " + secondArg);
// This code concatenates two command line arguments and prints them in a formatted string.
// It is useful for creating dynamic messages based on user input in command line applications. 